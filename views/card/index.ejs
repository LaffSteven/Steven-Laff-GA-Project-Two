<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>Arcane Planes | Card List</title>
    </head>
    <body>
        <h1>List of cards</h1>

        <div class="list-container">
            <ul id="hp-card-list">
                <% for (let i = 0; i < cardList.length; i++) {%>
                    <li> <a href="#"><%=cardList[i].name%></a> </li>
                <% } %>
            </ul>
        </div>
    </body>
</html>
